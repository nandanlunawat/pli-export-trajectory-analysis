# Genuine dependencies of the final pipeline (see 05_analysis/).
# Versions used for the frozen results: Python 3.10, statsmodels 0.15.0.
# statsmodels is pinned because ARMA/ETS/SARIMAX numerical output can vary
# across versions; the rest are pinned loosely.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
statsmodels==0.15.0
scipy>=1.10
ruptures>=1.1        # Bai-Perron-style break diagnostic (robustness notebook)
requests>=2.28       # fetch_meidb.py only (data re-extraction)

# Needed only to run the notebooks:
jupyter
nbconvert
ipykernel
